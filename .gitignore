# Cloned upstream reference repos — provenance lives in references/<name>.md (tracked).
# Set up locally with the commands in references/README.md.
references/gearmulator/
references/edisyn/
references/mwsd/
references/MidiKraft-librarian/

# Local gearmulator source snapshot for the Xenia hardware-bridge work — large, not redistributed.
resources/

# Waldorf reference PDFs and derived text — kept locally only, NOT redistributed.
# See docs/spec/README.md for the public Waldorf download URL.
docs/spec/*.pdf
docs/spec/*.txt
references/**/*.pdf

# macOS junk
.DS_Store

# Editor / IDE
.idea/
.vscode/
*.swp

# Build outputs
build/
cmake-build-*/

# Stray macOS/Xcode artifacts
esp_jit.log
